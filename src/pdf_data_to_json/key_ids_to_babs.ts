// import { curry } from 'lodash';
// import { Bab } from './../legal/structure/bab';
// import { Bab } from '../legal/structure/bab';
// import { lastOf, Span } from '../util';
// import { BabKeySpanIds } from './babs_spans_to_key_ids';

// export function keyIdsToBabs(spanKeyIds: BabKeySpanIds, spans: Span[]): Bab[] {
//   const initial: Acc = { keySpans: []};
//   const a = spans.reduce(toBabsSpansWith(spanKeyIds), initial);
// }

// type Acc = {
// keySpans: {[key: number]: Span[]}
// currentKey?: number;
// };

// const toBabsSpansWith = curry(toBabsSpans);

// export function toBabsSpans(spanKeyIds: BabKeySpanIds, babSpans: Acc, span: Span, ): Acc {
//   const
//   const newBabSpans: BabSpans[] = [...]
//   return [];
// }

// function babOfSpans(spans: Span[]): Bab[] {

// }
